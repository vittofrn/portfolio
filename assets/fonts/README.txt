Put your typeface here, renamed to:

  display.woff2          <- best. convert your .otf/.ttf at cloudconvert.com/otf-to-woff2
  display.otf            <- or this
  display.ttf            <- or this
  display-italic.woff2   <- optional

The site picks it up automatically. Until then it falls back to a system sans.
